# Tasks: Admin Exam Plan Management

- [x] Task 1: 實作考試分項總覽列表。
  - **驗收標準**: 進入 `#/admin/exam-plans` 時，看到 Mock 中的設定檔以表格呈現。並且程式邏輯能比較 `開始時間` 與 `結束時間` 與當下時間 (`new Date()`) 的大小，動態顯示「🟢 開放中」或「🔴 已結束」的 Badge。
- [x] Task 2: 實作編輯時間窗 Modal 與防呆邏輯。
  - **驗收標準**: 開啟編輯視窗修改開放/結束時間時，如果結束時間早於開始時間，會觸發紅字錯誤提示並禁用「儲存」按鈕；修復後才能儲存。
- [x] Task 3: 與系統連動驗證。
  - **驗收標準**: 在這裡修改了某次考試的時間讓他「過期」後，切換回首頁 `#/` (教師 Dashboard)，原本列出來的代辦事項卡片必須自動消失（代表時間遮罩確實生效）。

- [x] Task: 更新文件
  - **驗收標準**: 實作完成後，務必更新 `README.md` 的相關說明；如果涉及架構或業務邏輯的改變，請在 `docs/` 當中加入合適的文件。
